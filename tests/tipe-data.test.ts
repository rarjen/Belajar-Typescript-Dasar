describe("Data Type", () => {
  it("must declare", () => {
    const name: string = "Otniel Kevin Abiel";
    const balance: number = 10000;
    const isVip: boolean = true;

    console.log(name);
    console.log(balance);
    console.log(isVip);

    // name = 100; // type error
  });
});

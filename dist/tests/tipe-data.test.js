"use strict";
describe("Data Type", () => {
    it("must declare", () => {
        const name = "Otniel Kevin Abiel";
        const balance = 10000;
        const isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
        // name = 100; // type error
    });
});
